# unit

A unit in rust is a type that represents the absence of a value. -> It is denoted by the keyword `()`.

# prelude

A prelude is a collection of commonly used modules and types that are automatically imported into every Rust program. -> E.g., `println!`, `Vec`, `HashMap`, etc.
